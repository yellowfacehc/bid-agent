# 招投标信息查询平台 - 部署指南

## 一、项目文件说明

```
bid_agent/
├── app.py              # Web应用主程序（启动后可通过浏览器访问）
├── bid_agent.py        # 命令行工具（在终端中使用）
├── requirements.txt    # Python依赖包列表
├── README.md           # 项目说明
├── 部署指南.md          # 本文件
└── crawlers/
    ├── __init__.py     # 模块导出
    ├── base.py         # 基类（数据结构+通用方法）
    ├── ccgp.py         # 中国政府采购网爬虫
    ├── ggzy.py         # 全国公共资源交易平台爬虫
    └── cebpub.py       # 中国招标投标公共服务平台爬虫
```

---

## 二、本地运行（最快上手）

适合：个人使用、测试体验

### 第1步：安装 Python
- 前往 https://www.python.org/downloads/ 下载 Python 3.10 或以上版本
- 安装时勾选 "Add Python to PATH"

### 第2步：下载项目文件
- 下载 bid_agent_project.tar.gz 压缩包
- 解压到任意文件夹，例如 `C:\bid_agent` 或 `~/bid_agent`

### 第3步：安装依赖
打开终端（Windows用CMD/PowerShell，Mac用Terminal）：

```bash
cd bid_agent
pip install -r requirements.txt
```

### 第4步：启动Web应用
```bash
python app.py
```

### 第5步：浏览器访问
打开浏览器，访问 http://localhost:5000

其他人在同一局域网内可通过你的IP地址访问（如 http://192.168.1.100:5000）

---

## 三、部署到云服务器（让所有人都能访问）

适合：对外提供服务、多人使用

### 方案A：阿里云/腾讯云 ECS 服务器

1. 购买一台云服务器（推荐 Ubuntu 22.04，最低配置1核2G即可）

2. 登录服务器，安装环境：
```bash
# 安装 Python
sudo apt update
sudo apt install python3 python3-pip -y

# 上传项目文件（可用 scp 或宝塔面板上传）
# 安装依赖
cd /opt/bid_agent
pip3 install -r requirements.txt

# 测试运行
python3 app.py
```

3. 使用 Gunicorn 正式部署（保持后台运行）：
```bash
pip3 install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 app:app --daemon
```

4. 配置 Nginx 反向代理（可选，使用80端口）：
```nginx
server {
    listen 80;
    server_name your-domain.com;
    location / {
        proxy_pass http://127.0.0.1:5000;
    }
}
```

5. 在云服务器安全组中开放 5000 端口（或80端口）

6. 其他用户通过 http://你的服务器IP:5000 即可访问

### 方案B：Docker 容器部署

1. 创建 Dockerfile：
```dockerfile
FROM python:3.10-slim
WORKDIR /app
COPY . /app
RUN pip install -r requirements.txt
EXPOSE 5000
CMD ["python", "app.py"]
```

2. 构建并运行：
```bash
docker build -t bid-agent .
docker run -d -p 5000:5000 --name bid-agent bid-agent
```

### 方案C：免费托管平台

- **PythonAnywhere**: https://www.pythonanywhere.com/ （免费版可托管Flask应用）
- **Render**: https://render.com/ （免费版支持Python Web应用）
- **Railway**: https://railway.app/

以 Render 为例：
1. 将代码推送到 GitHub 仓库
2. 在 Render 创建 Web Service，选择该仓库
3. 构建命令：`pip install -r requirements.txt`
4. 启动命令：`python app.py`
5. Render 自动分配一个公网URL，任何人都能访问

---

## 四、扩展为手机APP/小程序

当前项目是一个 Web 应用，要变成手机APP有几种路径：

### 路径1：直接用手机浏览器访问（零成本）
- 部署到云服务器后，手机浏览器直接访问URL
- 可添加到手机桌面，使用体验接近APP

### 路径2：封装为 APP（中等成本）
使用工具将Web应用打包为原生APP：
- **HBuilder/uni-app**: 可将Web页面打包为Android/iOS应用
- **Cordova/PhoneGap**: 将Web应用包装为移动APP
- **微信小程序**: 需将前端页面改写为小程序组件，后端API不变

### 路径3：开发原生APP（高成本）
需要使用 Flutter/React Native 开发前端界面，后端调用现有的 `/api/search` 接口。

---

## 五、当前功能与限制

### 已实现功能
- 关键词搜索招投标信息
- 按地区筛选（31个省市）
- 按日期范围筛选
- 搜索结果展示（标题、日期、地区、公告类型、采购人、链接）
- Web界面 + REST API

### 数据来源
- 中国政府采购网（稳定可用）
- 全国公共资源交易平台（接口已失效，平台升级了验证码系统）
- 中国招标投标公共服务平台（接口加密，暂不支持）

### 已知限制
- 搜索速度约10-30秒（受目标网站反爬延迟限制）
- 目前仅中国政府采购网数据源稳定
- 单次搜索最多返回100条（5页×20条）

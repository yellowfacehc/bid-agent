# 招投标Agent v5.6 更新说明

## v5.6 修复内容（2026-08-28）

### 1. 【核心修复】中国政府采购网爬虫通过Cloudflare代理时改用真正的POST请求
- **问题根因**：之前的代码通过Cloudflare Worker代理时，将POST参数拼接到URL查询字符串中，用GET方式请求。
  但中国政府采购网的列表API（/cgyx/pub/pubSearchData）只接受POST请求，GET方式返回空或错误，
  导致中国政府采购意向始终返回0条。
- **修复方案**：
  - 通过Cloudflare Worker代理时，使用真正的POST请求（requests.post），将POST数据作为请求体发送
  - Cloudflare Worker（cloudflare-worker.js）已支持POST请求转发，会将请求体转发到目标URL
  - 直接POST返回403/451/400时，也触发代理转发（之前只在ConnectionError时触发）

### 2. 修复直接POST返回403时未触发代理的问题
- 之前只有ConnectionError或Timeout时才触发CORS代理转发
- 现在HTTP 403/451/400（地域限制或防火墙拦截）也会触发代理转发

---

## v5.5 修复内容（2026-08-28）

### 1. 【核心修复】去掉区名关键词搜索，改用空关键词翻10页+地理归属过滤
- **问题根因**：之前的逻辑是"空关键词 + 区名关键词（九龙坡/大渡口）"同时搜索。
  但API的区名关键词搜索只能返回标题含区名的项目：
  - "九龙坡"关键词：仅返回9条（第2页起为0）
  - "大渡口"关键词：仅返回4条
  - 合计仅13条，与用户搜到的结果一致！
  而Excel中很多项目标题不含区名（重庆开放大学、电力高专、红光中学等），这些只能靠空关键词搜索+地理归属过滤。
- **修复方案**：
  - 去掉区名关键词搜索，只用空关键词搜索
  - 空关键词时翻10页（100条），获取更多历史数据
  - 然后用地理归属智能过滤，筛选出九龙坡区/大渡口区的项目
  - 这样能覆盖标题不含区名但实际归属目标区县的项目

### 2. 修复去重逻辑（按标题去重，保留最新一条）
- **问题**：之前按"标题+URL"去重，但同一标题可能多次发布（bizId不同），导致重复出现
  （如九龙坡区人民法院重复出现3次）。
- **修复**：改为按标题去重，同一标题保留发布日期最新的一条。

### 3. 已知问题：中国政府采购网爬虫返回0条
- Excel明细表中有14条来自中国政府采购网（cgyx.ccgp.gov.cn），但系统搜不到
- 原因：海外服务器直接访问被403，POST API代理中转可能不可用
- 后续计划：专门调试中国政府采购网爬虫

---

## v5.4 修复内容（2026-08-28）

### 1. 补充地理数据库（基于实际采购明细表）
根据用户提供的55条九龙坡区+大渡口区采购意向明细表，补充了以下单位：
- **九龙坡区学校**：重庆开放大学、重庆工商职业学院、重庆电力高等专科学校、
  重庆市九龙坡职业教育中心、重庆市九龙坡区特殊教育学校
- **九龙坡区医院**：重庆医科大学附属康复医院、重庆医科大学附属第一医院、重医附一院
- **验证**：新增单位全部能正确归属到九龙坡区/大渡口区

### 2. 增加翻页数量
- 采购意向搜索翻页从3页增加到5页，获取更多历史数据
- 2关键词→3页，3关键词→2页，1关键词→5页

### 3. 已知问题：中国政府采购网爬虫返回0条
- Excel明细表中有14条来自中国政府采购网（cgyx.ccgp.gov.cn），但系统搜不到
- 原因：海外服务器直接访问被403，POST API通过代理中转可能不可用
- 后续计划：专门调试中国政府采购网爬虫，优化代理中转逻辑

---

## v5.3.1 修复内容（2026-08-28）

### 1. 修复搜索超时导致前端报错 "Unexpected end of JSON input"
- **问题根因**：空关键词+目标区县搜索时，触发2个关键词（"" + "九龙坡"）× 2个平台 × 5页，
  加上"九龙坡"关键词还会抓取详情页（最多20条），总请求数太多，超过Render免费版60秒超时限制，
  连接被断开，前端收到空响应，解析JSON失败。
- **优化方案**：
  1. 区名关键词（"九龙坡"、"大渡口"）不抓取详情页——标题中已包含区名，无需全文匹配
  2. 详情页抓取数量从20条减少到10条
  3. 翻页数量从5页减少到3页（2关键词→2页，3关键词→1页）
- **效果**：总请求数减少约60%，搜索时间控制在30-40秒内，避免Render超时

---

## v5.3 修复内容（2026-08-28）

### 1. 修复空关键词+目标区县搜索返回0条的问题
- **问题根因**：空关键词搜索时，API只返回最近的10条（第一页），而这些恰好都是市级单位
  （农业农村委、食药检院等），不属于九龙坡区/大渡口区，所以地理归属过滤后是0条。
- **解决方案**：当关键词为空且选择了目标区县时，自动用区名作为补充关键词搜索API：
  - 用户选了九龙坡区 → 自动搜索"九龙坡"
  - 用户选了大渡口区 → 自动搜索"大渡口"
  - 同时保留空关键词搜索（获取最近的所有结果）
  - 两者合并去重后，再做地理归属智能过滤
- **效果**：能直接命中标题中包含"九龙坡"/"大渡口"的项目（如九龙坡区人民法院、大渡口区规划局等）

### 2. 地理归属增加区名关键词匹配
- 之前设计是"不依赖标题中的区名关键词"，但实际很多项目标题明确写了区名
- 现在增加`district_names`匹配类别（权重40），标题含"九龙坡区"/"大渡口区"时加分
- 区名匹配作为补充依据，不影响道路/学校/机构等地理特征的高权重匹配

### 3. 补充大渡口区地理数据库
- 新增政法单位地标：大渡口区人民法院、大渡口区检察院、大渡口区公安分局、大渡口区司法局
- 新增区名关键词：大渡口区、大渡口

### 4. 增加采购意向翻页数量
- 默认页数从3页增加到5页，获取更多历史数据（如4月的红光中学项目）
- 多关键词时也适当增加页数（2关键词→3页，3关键词→2页）

---

## v5.2.3 修复内容（2026-08-28）

### 1. 彻底修复采购意向模式下日期选择器消失问题
- **v5.2.2方案的问题**：虽然只隐藏`.region-group`（区县下拉），但如果用户部署了旧版本或浏览器缓存了旧JS，
  旧代码`document.querySelector('.filter-row').style.display = 'none'`仍会隐藏整个filter-row（包括日期）。
- **v5.2.3彻底方案**：把日期选择器从filter-row中**完全独立出来**，放在关键词和目标区县之间：
  - `filter-row`：只包含区县下拉（采购意向模式下隐藏）
  - `date-row`：独立的日期选择器行（两种模式下始终可见，不受任何切换逻辑影响）
  - `intentionSection`：目标区县复选框（采购意向模式下显示）
- 这样即使旧JS代码隐藏了filter-row，日期选择器也不会受影响。

---

## v5.2.2 修复内容（2026-08-28）

### 1. 修复采购意向模式下日期选择器消失问题
- **问题根因**：切换到采购意向模式时，`document.querySelector('.filter-row').style.display = 'none'`
  隐藏了整个筛选行，而`.filter-row`包含了区县下拉+开始日期+结束日期三个字段，导致日期选择器也被隐藏。
- **修复方案**：
  - 给区县下拉的form-group添加`class="region-group"`
  - 切换模式时只控制`.region-group`的显示/隐藏，日期选择器始终可见
  - 普通搜索模式：显示区县下拉；采购意向模式：隐藏区县下拉（用目标区县复选框替代）

### 2. 默认日期范围改为最近30天
- 采购意向更新频率低，原默认最近20天可能数据过少
- 统一为最近30天（与后端采购意向默认一致）

---

## v5.2.1 修复内容（2026-08-28）

### 1. 修复空关键词搜索报错 "max_workers must be greater than 0"
- **问题根因**：后端API将空关键词转为 `[""]` 传给bid_agent，但bid_agent.py中
  `valid_keywords = [kw.strip() for kw in keywords if kw and kw.strip()]` 又把空字符串过滤掉，
  导致 `valid_keywords=[]`，然后 `ThreadPoolExecutor(max_workers=0)` 抛出错误。
- **修复方案**：
  - 当 `valid_keywords` 为空时，设置为 `[""]`（空关键词表示搜索全部）
  - `max_workers` 计算增加下限 `max(..., 1)`，确保至少为1
  - 普通招投标搜索和采购意向搜索两处均已修复

---

## v5.2 修复内容（2026-08-28）

### 1. 修复空关键词无法搜索问题
- 前端已移除"请输入至少一个关键词"的alert校验（v5.1已注释，确认部署v5.2版本即可生效）
- 后端支持关键词为空时，按日期范围返回所有采购意向

### 2. 修复关键词搜索不到详情页内容问题（核心修复）
- **问题根因**：重庆政府采购意向API的title参数只匹配**标题**，不匹配详情内容。
  例如"重庆市大坪监狱基础设施数字化项目"的标题是"重庆市大坪监狱2026年7月政府采购意向"，
  标题中没有"数字化"，所以搜"数字化"时搜不到。
- **解决方案**：新增详情页并发抓取功能
  - 获取列表后，对每个项目抓取详情页，提取"采购项目名称"和"采购需求概况"
  - 在客户端做全文关键词匹配（标题 + 详情内容 + 采购单位）
  - 并发数5，最多抓取20条详情页（避免超时）
  - 空关键词时不抓取详情页，直接返回列表（节省时间）

### 3. 补充地理数据库
- 九龙坡区新增地标：重庆市大坪监狱、大坪监狱、重庆市九龙监狱、九龙监狱、九龙坡区法院、九龙坡区检察院
- 九龙坡区新增道路：石油路（横跨渝中区和九龙坡区，渝州路街道段属九龙坡区）
- 大坪监狱归属验证：匹配"渝州路街道"+"重庆市大坪监狱"+"渝州路"，得分220，正确归属九龙坡区

---

## v5.1 修复内容（2026-08-27）

### 1. 修复重庆采购意向爬虫无结果问题
- **问题**: 搜索"数字化""信息化"等关键词返回0条，但实际网站有数据
- **原因**: API请求缺少 `regionId=130117562645086249`（重庆市ID）参数，导致API返回空
- **修复**: 在 `crawlers/intention_cq.py` 中补上 `regionId`、`excludeRegionId`、`queryAddCount` 等必要参数

### 2. 修复中国政府采购意向海外服务器403问题
- **问题**: Render海外服务器直接访问 cgyx.ccgp.gov.cn 返回 HTTP 403
- **原因**: 爬虫使用了独立的 `requests.Session()`，没有走 BaseCrawler 的代理中转机制（CORS_PROXY_URL / CRAWL_PROXY）
- **修复**: 重写 `crawlers/intention_ccgp.py`，使用 `_safe_get()` 和带代理的 `_post_via_proxy()` 方法，自动降级走 Cloudflare Worker 代理

### 3. 支持无关键词搜索
- **问题**: 不输入关键词就报错"关键词不能为空"
- **修复**: 前后端均移除关键词必填校验
  - 关键词为空时，按日期范围返回该时间段内的**所有采购意向**
  - 普通招投标搜索也同步支持无关键词查询

### 4. 解决区域筛选冲突
- **问题**: 页面上同时有"区县"下拉（全重庆/各区县）和"目标区县"复选框（九龙坡/大渡口），用户困惑
- **修复**: 两种搜索模式自动切换显示
  - **普通招投标搜索模式**: 显示"区县"下拉，隐藏"目标区县"复选框
  - **采购意向搜索模式**: 隐藏"区县"下拉，显示"目标区县"复选框（地理归属智能过滤）
  - 点击对应搜索按钮时自动切换，无需手动操作

---

## v5.0 新增内容

### 1. 新增采购意向专属爬虫（2个）

#### 重庆市政府采购网采购意向
- **文件**: `crawlers/intention_cq.py`
- **API**: `GET /yw-gateway/zcjquery/v1/website-content-aggregations/front`
- **关键参数**: `typeCode=aggregation-purchaseintention`（与招标公告共用API端点，仅typeCode不同）
- **详情页URL**: `https://www.ccgp-chongqing.gov.cn/stock-resources/front/intentionView?id={bizId}/front?__platDomain__=www.ccgp-chongqing.gov.cn`

#### 中国政府采购网采购意向
- **文件**: `crawlers/intention_ccgp.py`
- **列表页**: `GET http://cgyx.ccgp.gov.cn/cgyx/pub/pubSearch`
- **分页API**: `POST /cgyx/pub/pubSearchData`（表单参数，需先GET获取session）
- **详情页URL**: `http://cgyx.ccgp.gov.cn/cgyx/pub/details?groupId={groupId}`
- **特点**: 全国各省市采购意向汇总（包含重庆），搜索表单有验证码但直接调用分页API不需要验证码

### 2. 新增地理归属判断模块

- **文件**: `region_geo.py`
- **功能**: 通过道路、学校、医院、商圈、街道等地理特征智能判断采购意向归属的区县
- **不依赖标题中的"九龙坡""大渡口"关键词**
- **支持区域**: 九龙坡区、大渡口区（可扩展至其他区县）

#### 匹配优先级（权重从高到低）
1. 行政街道/镇（100分）- 如"杨家坪街道""新山村街道"
2. 医院全称（90分）- 如"重钢总医院"
3. 学校全称（80分）- 如"重庆市红光中学""重庆市第三十七中学"
4. 商圈/地标/园区（70分）- 如"建桥工业园""二郎科技新城"
5. 社区/居委会（60分）
6. 道路名称（50分）- 如"渝州路""钢花路"

#### 数据库内容
- **九龙坡区**: 9个街道+10个镇、200+道路、60+学校、20+医院、50+商圈地标、40+社区
- **大渡口区**: 5个街道+3个镇、100+道路、25+学校、15+医院、40+商圈地标、35+社区

#### 测试用例（全部通过）
```
✓ 重庆市红光中学2026年政府采购意向 → 九龙坡区（学校匹配）
✓ 重庆市第三十七中学信息化建设意向 → 大渡口区（学校匹配）
✓ 渝州路75号办公楼维修意向 → 九龙坡区（道路匹配）
✓ 钢花路123号商铺装修意向 → 大渡口区（道路匹配）
✓ 建桥工业园企业服务采购意向 → 大渡口区（园区匹配）
✓ 某学校设备采购意向 → 无（无地理特征，不强行归属）
```

### 3. 前端UI更新

#### 新增"采购意向专属搜索"按钮
- 与"搜索招投标公告"按钮并列，橙色渐变区分
- 悬停或点击后显示目标区县选择区域

#### 新增目标区县多选（采购意向专用）
- 九龙坡区、大渡口区两个复选框
- 勾选后只返回归属该区县的采购意向（通过地理信息智能判断）
- 不勾选返回全部采购意向

#### 新增搜索模式提示
- 显示当前数据源：重庆政府采购网 + 中国政府采购网
- 结果列表标注"采购意向"类型

### 4. 后端API更新

#### 新增 `/api/search-intention` 接口
- **方法**: POST
- **参数**:
  - `keywords`: 关键词列表
  - `start_date` / `end_date`: 日期范围
  - `region`: 地区筛选
  - `max_pages`: 最大爬取页数
  - `target_districts`: 目标区县列表（如 `["九龙坡区", "大渡口区"]`）
- **返回**: 与 `/api/search` 格式一致，增加 `target_districts` 字段

### 5. Agent核心更新

#### `BidAgent` 类新增
- `intention_crawlers` 属性：管理2个采购意向爬虫
- `search_intention()` 方法：采购意向专属搜索，支持地理归属过滤
- `_search_intention_single()` 方法：单关键词并行搜索2个意向平台

## 文件清单

### 新增文件
- `region_geo.py` - 地理归属判断模块
- `crawlers/intention_cq.py` - 重庆政府采购意向爬虫
- `crawlers/intention_ccgp.py` - 中国政府采购意向爬虫

### 修改文件
- `crawlers/__init__.py` - 导出新爬虫
- `bid_agent.py` - 新增采购意向搜索方法和地理归属过滤
- `app.py` - 新增采购意向API路由、前端按钮、目标区县选择

## 使用方式

### 普通招投标搜索（不变）
点击"搜索招投标公告"按钮，搜索4个平台的招标/中标/采购公告。

### 采购意向专属搜索（新增）
1. 输入关键词（如"信息化""设备采购"）
2. （可选）勾选目标区县：九龙坡区 / 大渡口区
   - 勾选后系统会通过单位名称、地址、道路、学校等地理特征智能判断归属
   - 即使标题中没有"九龙坡"或"大渡口"，也能正确归属（如"重庆市红光中学"→九龙坡区）
3. 点击"采购意向专属搜索"按钮
4. 系统搜索2个采购意向平台（重庆政府采购网 + 中国政府采购网）

### 命令行使用
```python
from bid_agent import BidAgent

agent = BidAgent()

# 搜索采购意向（全部地区）
items = agent.search_intention(keywords=["信息化"], days=30)

# 搜索采购意向（仅九龙坡区和大渡口区，地理归属智能判断）
items = agent.search_intention(
    keywords=["信息化"],
    days=30,
    target_districts=["九龙坡区", "大渡口区"]
)
```

## 部署注意事项

1. 中国政府采购网(cgyx.ccgp.gov.cn)是HTTP协议，海外服务器可能需要通过CORS代理访问
2. 采购意向搜索默认查询最近30天（比招投标公告的20天长，因为意向发布较早）
3. 多关键词搜索时自动减少页数，避免超时
4. 地理归属判断在客户端完成，不依赖外部API
